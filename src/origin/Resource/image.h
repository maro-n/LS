
#pragma once
#include "../common.h"

//
// 画像リソース管理
//

/*
class cImageResource {
  static bool get_image_;

public:

  // キャラクターの画像データ生成
  // TIPS: 使用する画像のIDを入力、アドレスを出力する
  static void getInstance() {
    if (!get_image_) {
      data[0] = Texture("res/png/unit_fighter.png");
      data[1] = Texture("res/png/unit_scout.png");

      get_image_ = true;
    }
  }

  static Texture data[type::TypeVariation];
};

typedef cImageResource  image;
*/
